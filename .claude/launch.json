{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "FastAPI dev server",
      "runtimeExecutable": "uvicorn",
      "runtimeArgs": ["app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--log-level", "info"],
      "port": 8000
    }
  ]
}
