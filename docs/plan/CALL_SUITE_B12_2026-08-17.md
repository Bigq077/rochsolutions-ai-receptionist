# Call suite — B1.2 / `U-07` · 2026-08-17

**Clinic / dial:** `jv_v2` · **`+447367002651`**
**Build must read:** `[build_info] running build 66dd7a1` in the Render log
**SMS:** leave `SMS_ENABLED` **off**
**Time:** ~20 minutes, 5 calls

**The one rule:** check the **diary**, not what she said. Both defects this suite
exists to close sounded completely correct on the call.

---

## Pre-flight (do not skip)

1. Render log for the `jv_v2` service → find `[build_info] running build <sha>`
   at the end of any call.
2. It must say **`66dd7a1`**. If it says `9b369bf`, the deploy has not landed —
   **stop**, nothing below proves anything.
3. Open the Google calendar Susie writes to.
   ⚠️ **Not Carepatron.** JV's Carepatron sync is bound to the *primary*
   calendar and Susie books to the *secondary* — a correct booking will not
   appear in Carepatron. Checking there will read as a failure that isn't one.

---

## Call 1 — set up (and a free sanity check)

Book an ordinary appointment. Any name, any slot a few days out.

- [ ] It lands in the diary, right name, right day, right time
- [ ] SID: `________________`

Keep this appointment. Calls 2–4 move it.

---

## Call 2 — the recorded defect (A9b) ⭐ **the one that matters**

Say you need to move the appointment you just booked. Let her offer days, pick
one, and wait for her to ask **"shall I go ahead and move it?"**

Then **say nothing for ~10 seconds.**

- [ ] She re-asks the **move** confirmation — something like
      *"Still with you — shall I go ahead and move it?"*
- [ ] ❌ **FAIL** if she asks *"which of those days suits you?"* — that is the
      original bug, unfixed
- [ ] ❌ **FAIL** if she asks *"shall I go ahead?"* with no "move it" — that is
      the bug I introduced; it means `36a7e5b` did not deploy

Now say **"yes"**.

- [ ] **Check the diary.** The appointment actually moved.
- [ ] ❌ **FAIL** if she sounded like it worked and the diary is unchanged —
      this is the exact failure mode, and it is invisible on the call
- [ ] SID: `________________`

---

## Call 3 — options and CTA in one breath, picked by **voice**

Reschedule again. You are looking for a turn where she reads out numbered
options **and** asks the confirmation in the same reply — *"Number 1 … Number 2 …
shall I go ahead and move it?"*

If she splits them across two turns, that's fine — try once more, then move on
and note it.

When you get the combined turn, say **"number two"**.

- [ ] She accepts it and proceeds
- [ ] ❌ **FAIL** if she ignores it, asks again, or goes quiet — that is the
      defect `3b6695e` fixed
- [ ] Diary reflects option 2
- [ ] SID: `________________`

---

## Call 4 — same turn, picked by **keypad**

Same as call 3, but when the combined turn comes, **press `2`** instead of
speaking.

The keypad path only arms after she suggests it, so if she never mentions the
keypad, say *"sorry, you're breaking up"* once to trigger it, then press `2`.

- [ ] The keypress registers as option 2
- [ ] ❌ **FAIL** if nothing happens — the slot map was cleared
- [ ] SID: `________________`

---

## Call 5 — withheld keypad (B1.1, also awaiting proof)

Dial with **`#31#`** in front: `#31#+447367002651`. Book, and go to the phone
number step.

- [ ] You hear **"I can't see a phone number on this call"** *before* she asks
      you to type it
- [ ] ❌ **FAIL** if she jumps straight to "type the number on your keypad"
- [ ] SID: `________________`

---

## Cleanup

- [ ] **Delete every test booking** from the diary
- [ ] Paste the SIDs + build SHA against `U-07` in `REGISTER_B_U.md`

---

## What each result means

| Outcome | What to do |
|---|---|
| Calls 2–4 all pass | `U-07` closes. Next: B1.2 (b) re-speak confirm and (c) post-write closing — but see below |
| Call 2 passes, and you **heard the confirmation clearly** | (b) re-speak-confirm may not be needed at all. Say so — it was queued off the original barge-cut symptom |
| Call 2 fails on the diary | **Stop and revert.** `git push --force origin 9b369bf:jv_v2` — the caller believes a move happened that didn't |
| Call 3 or 4 fails | The narrowing didn't hold. Send me the SID; the obs turns show whether the map survived |
| She never combines options + CTA in one turn | Note it — it means the defect was rarer than assumed, not that it's absent |

Anything ambiguous: paste the SID. The obs turns show what was actually spoken
and what state was set, which is the seam where the last two defects hid.
