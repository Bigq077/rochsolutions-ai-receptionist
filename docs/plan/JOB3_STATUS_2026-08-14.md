# Job 3 status — 2026-08-15

**Priority:** Job 3 (`jv_v2`) ahead of the 50 adversarial dials.  
**Dial:** `+447367002651` · confirm `[build_info] running build` after deploy.

## Shipped + call-proven

| Item | Canonical | `jv_v2` | Revert | Call proof |
|---|---|---|---|---|
| **3c.4** day-only hold clip | `445fff1` | `2969ccc` | `85947b1` | **PASS** `CA8a08105defd4d96477a559d2d8eb8b82` |
| **3a** reason before timing after `Right —` | `8c533d4` | `1537fa8` | `2969ccc` | **PASS** `CA47a74aeca19e068ac775a3b429ec6878` |
| **3c.5** strip “time preference noted” | `6d7c2ec` | `8adce54` | `1537fa8` | **PASS** `CAd15c3af6fc73480fe9dbe2df81c7bd6d` (“right,” then moves on) |
| **3c.1** duplicate `check_availability` on accept | `e0d914e` | `c8dcd05` | `8adce54` | **PASS** `CA5db4ea0cf96d4c4ea272d5c8dad80315` (asked which, named by day; no full re-list) |
| **3c.2** out-of-window offer acknowledgement | `14eba0e` | `a9155dd` | `d6813ac` | **PASS** `CAc875c27a7500df568f0942e81281fe63` (day 1 in-window; day 2 had 5pm as leftover — not the silent half-four defect) |

Root cause for 3a: injector in `connection.py` always queued timing; prompt 1b never ran on that path. Opt-in clinics now get `prompt_facts.reason_question` first.

## Shipped — awaiting call proof

| Item | Canonical | `jv_v2` | Revert | Notes |
|---|---|---|---|---|
| **3c.3** keep pre-slot empathy (clinic-gated) | *(pending)* | — | `c3d9ea3` | `prompt_facts.keep_pre_slot_speech` on jv_v1 only; engine suppress stays default elsewhere. |

**Call script (3c.3):** mention a condition + book in one breath (e.g. “I’ve hurt my ankle, can I book?”). Pass if you hear a short empathy / physio line *before* slots (or before the hold). Fail if logs show `pre-slot chunk suppressed` for that line and you never heard it.

## Verified without a call

| Item | Result |
|---|---|
| **3b** SMS on/off suite | `test_sms_call_mode_toggle.py` — **36 passed** on `jv_v2`. Live exercise blocked (`SMS_ENABLED` off by Quentin). `call_overflow.enabled=false`, Marcus `+447586605462`. |
| **3c.7** `digest.email_to` | Empty on jv_v1 — Carepatron handoff still needs a recipient from Quentin/Marcus. |

## Still open (next)

1. Call-prove **3c.3**.
2. **3c.6** `SHEETS_ENABLED` on Render (env; Quentin finishes sheet).
3. **3d** booking-outcome fallback call exercise.

## Parked

Adversarial 50 / today’s 25-call sheet — resume after Job 3 critical path.
