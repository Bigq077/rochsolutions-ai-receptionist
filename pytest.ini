[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live_sms: test is allowed to send a REAL text. Opt-in only; see the block_outbound_sms fixture in tests/conftest.py. Should essentially never be used.
