[pytest]
asyncio_mode = auto
testpaths = tests

# Integration tests hit live credentials/network (Twilio SMS, Acuity, Google
# Sheets) and are excluded by default so CI and local runs stay offline.
# Run them explicitly with:  pytest -m integration
addopts = -m "not integration"
markers =
    integration: test requires live credentials or network (excluded from CI)
